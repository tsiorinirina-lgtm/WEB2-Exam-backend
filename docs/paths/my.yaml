/my/exams:
  get:
    tags: [My]
    summary: Examens disponibles pour l'Ã©tudiant connectÃ© (fenÃªtre ouverte, non passÃ©s)
    responses:
      "200":
        description: Liste possiblement vide
        content:
          application/json:
            schema:
              type: array
              items: { $ref: "../components/schemas.yaml#/schemas/MyExam" }
      "401": { $ref: "../components/securitySchemes.yaml#/responses/Unauthorized" }
      "403": { $ref: "../components/securitySchemes.yaml#/responses/ForbiddenStudent" }

/my/exams/{id}:
  parameters:
    - $ref: "../components/securitySchemes.yaml#/parameters/Id"
  get:
    tags: [My]
    summary: DÃ©tail d'un examen Ã  passer (sans is_correct â€” RG-07)
    responses:
      "200":
        description: Examen avec questions et choix (id et texte uniquement)
        content:
          application/json:
            schema: { $ref: "../components/schemas.yaml#/schemas/MyExamDetail" }
      "400": { $ref: "../components/securitySchemes.yaml#/responses/BadRequest" }
      "401": { $ref: "../components/securitySchemes.yaml#/responses/Unauthorized" }
      "403":
        description: FenÃªtre fermÃ©e ("Exam is not available") ou mauvais rÃ´le
        content:
          application/json:
            schema: { $ref: "../components/schemas.yaml#/schemas/Error" }
      "404": { $ref: "../components/securitySchemes.yaml#/responses/NotFound" }
      "409":
        description: Examen dÃ©jÃ  passÃ© ("Exam already taken")
        content:
          application/json:
            schema: { $ref: "../components/schemas.yaml#/schemas/Error" }

/my/exams/{id}/submit:
  parameters:
    - $ref: "../components/securitySchemes.yaml#/parameters/Id"
  post:
    tags: [My]
    summary: Soumettre ses rÃ©ponses (note calculÃ©e cÃ´tÃ© serveur â€” RG-05/06)
    description: >
      Les questions absentes du tableau valent 0 point (soumission partielle
      autorisÃ©e). Chaque question_id doit appartenir Ã  l'examen, chaque
      choice_id Ã  sa question, sans doublon de question_id.
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            required: [answers]
            properties:
              answers:
                type: array
                items:
                  type: object
                  required: [question_id, choice_id]
                  properties:
                    question_id: { type: integer, example: 1 }
                    choice_id: { type: integer, example: 3 }
    responses:
      "201":
        description: Tentative enregistrÃ©e, note et correction complÃ¨te (RG-12)
        content:
          application/json:
            schema:
              type: object
              properties:
                score: { type: integer, example: 2 }
                total_points: { type: integer, example: 5 }
                correction:
                  type: array
                  description: Une entrÃ©e par question de l'examen, dans l'ordre
                  items: { $ref: "../components/schemas.yaml#/schemas/CorrectionLine" }
      "400":
        description: RÃ©ponses invalides (choix d'une autre question, question d'un autre examen, doublon, tableau manquant)
        content:
          application/json:
            schema: { $ref: "../components/schemas.yaml#/schemas/Error" }
      "401": { $ref: "../components/securitySchemes.yaml#/responses/Unauthorized" }
      "403":
        description: FenÃªtre fermÃ©e ("Exam is not available") ou mauvais rÃ´le
        content:
          application/json:
            schema: { $ref: "../components/schemas.yaml#/schemas/Error" }
      "404": { $ref: "../components/securitySchemes.yaml#/responses/NotFound" }
      "409":
        description: DÃ©jÃ  soumis ("Exam already taken" â€” RG-02)
        content:
          application/json:
            schema: { $ref: "../components/schemas.yaml#/schemas/Error" }

/my/results:
  get:
    tags: [My]
    summary: Historique des rÃ©sultats de l'Ã©tudiant connectÃ©
    responses:
      "200":
        description: Tentatives triÃ©es de la plus rÃ©cente Ã  la plus ancienne
        content:
          application/json:
            schema:
              type: array
              items:
                type: object
                properties:
                  exam_id: { type: integer }
                  title: { type: string }
                  course_code: { type: string, example: PROG2 }
                  score: { type: integer }
                  total_points: { type: integer }
                  submitted_at: { type: string, format: date-time }
      "401": { $ref: "../components/securitySchemes.yaml#/responses/Unauthorized" }
      "403": { $ref: "../components/securitySchemes.yaml#/responses/ForbiddenStudent" }
